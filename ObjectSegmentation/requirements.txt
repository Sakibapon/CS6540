# PyTorch with CUDA 12.1 support
# These three pull from the PyTorch wheel index so the C ops (NMS, ROI Align)
# are compiled with CUDA kernels
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1+cu121
torchvision==0.19.1+cu121
torchaudio==2.4.1+cu121

# Core scientific stack
numpy>=1.26,<2.1
matplotlib>=3.8
Pillow>=10.0
scipy>=1.11

# Jupyter kernel support so this env shows up in VSCode / JupyterLab
ipykernel>=6.29
ipywidgets>=8.1
jupyter>=1.0

# Helpful extras used across the course notebooks
tqdm>=4.66
pandas>=2.1
scikit-learn>=1.4
